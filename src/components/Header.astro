---
import ThemeToggle from './ThemeToggle.astro';

const navItems = [
  { href: '/', label: 'Home' },
  { href: '/projects/', label: 'Projects' },
  { href: '/homelab/', label: 'Homelab' },
  { href: '/blog/', label: 'Blog' },
  { href: '/career/', label: 'Career' },
  { href: '/resume/', label: 'Resume' },
  { href: '/contact/', label: 'Contact' },
];

const currentPath = Astro.url.pathname;
---

<header class="sticky top-0 z-50 border-b border-(--border) bg-(--bg)/90 backdrop-blur">
  <nav class="mx-auto flex max-w-6xl items-center justify-between gap-4 px-4 py-3 sm:px-6 lg:px-8" aria-label="Primary navigation">
    <a href="/" class="font-semibold tracking-normal text-(--text)">Marcus Whited</a>
    <div class="hidden items-center gap-1 md:flex">
      {
        navItems.map((item) => {
          const active = item.href === '/' ? currentPath === '/' : currentPath.startsWith(item.href);
          return (
            <a
              href={item.href}
              aria-current={active ? 'page' : undefined}
              class:list={[
                'rounded-md px-3 py-2 text-sm font-medium',
                active
                  ? 'bg-(--surface-strong) text-(--text)'
                  : 'text-(--muted) hover:bg-(--surface) hover:text-(--text)',
              ]}
            >
              {item.label}
            </a>
          );
        })
      }
    </div>
    <div class="flex items-center gap-2">
      <ThemeToggle />
    </div>
  </nav>
  <nav class="border-t border-(--border) px-4 py-2 md:hidden" aria-label="Primary navigation">
    <div class="nav-scroll mx-auto flex max-w-6xl gap-2 overflow-x-auto">
      {
        navItems.map((item) => {
          const active = item.href === '/' ? currentPath === '/' : currentPath.startsWith(item.href);
          return (
            <a
              href={item.href}
              aria-current={active ? 'page' : undefined}
              class:list={[
                'shrink-0 rounded-md px-3 py-2 text-sm font-medium',
                active
                  ? 'bg-(--surface-strong) text-(--text)'
                  : 'text-(--muted) hover:bg-(--surface) hover:text-(--text)',
              ]}
            >
              {item.label}
            </a>
          );
        })
      }
    </div>
  </nav>
</header>
